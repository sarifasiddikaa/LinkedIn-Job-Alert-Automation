# LinkedIn-Job-Alert-Automation
Automates the recruiter's morning routine: submit a job title/location/count once, and the workflow scrapes fresh LinkedIn postings, de-dupes them against what's already tracked, appends only the new ones to Google Sheets, and writes a short AI-generated hiring-trend summary — all through a single webhook call.
